specVersion: "v1beta"
serviceId: "pigs"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pigs-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
